// Compatibility endpoint for operational checks and clients that use the
// explicit billing-status URL. Authorization remains enforced by the canonical
// billing route.
export { GET } from "../route";
